# --- Project Info ---
spring.application.name=E-com_Project

# --- PostgreSQL Database Connection ---
spring.datasource.url=jdbc:postgresql://dpg-d1vq9aumcj7s73fl5pv0-a/ffupdate
spring.datasource.username=ffupdate_user
spring.datasource.password=GJd19brlyXAUfQKewgDnk5U7gmxEejPq
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA & Hibernate Settings ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- Initialize DB with SQL Scripts (if needed) ---
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

# --- Static File Location (for image/file serving) ---
spring.web.resources.static-locations=file:uploads/

# --- Optional Logging Level (debug Hibernate) ---
# logging.level.org.hibernate.SQL=DEBUG
# logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
